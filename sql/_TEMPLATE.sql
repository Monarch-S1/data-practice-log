-- ============================================================
-- Problem : <title>
-- Source  : StrataScratch #<id> | DataLemur <slug>
-- Concept : window functions
-- Level   : Easy | Medium | Hard
-- Date    : YYYY-MM-DD
-- Time    : <n> min
-- First attempt solved: yes | no
-- ============================================================

-- --- Question (in my own words) ------------------------------
-- Rewrite the prompt yourself. If you cannot restate it, you do
-- not understand it yet.


-- --- Solution ------------------------------------------------




-- --- What I learned ------------------------------------------
-- One or two lines. The specific thing, not "I learned window
-- functions". Example: "PARTITION BY resets the frame; ORDER BY
-- inside OVER() creates a running calculation, not a sort."


-- --- If failed: why -------------------------------------------
-- What did I reach for first, and why was it wrong?
-- Copy this file to sql/_retry/ and redo on <date + 3 days>.
